{
  "name": "paper-trading-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "echo 'Run: cd server && npm run dev' and 'cd client && npm run dev' in separate terminals"
  },
  "devDependencies": {
    "docx": "^9.6.1",
    "puppeteer": "^24.43.1"
  }
}
